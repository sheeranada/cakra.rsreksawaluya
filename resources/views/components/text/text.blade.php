<div>
    <div class="form-group">
        <label for="{{ $idfor }}" class="col-sm-2 col-form-label">{{ $label }}</label>
        <textarea class="form-control" id="{{ $idfor }}" rows="3" name="{{ $name }}" required>{{ $slot }}</textarea>
      </div>
</div>
